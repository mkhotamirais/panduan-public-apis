import NavTabs from "../../../layouts/NavMenus";

const tab = ["home", "product", "apa"];

const FakestoreApi = () => {
  return (
    <div>
      <NavTabs tab={tab} />
      FakestoreApi
    </div>
  );
};

export default FakestoreApi;
